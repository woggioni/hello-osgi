pluginManagement {
    repositories {
        maven {
            url = 'https://woggioni.net/mvn/'
        }
        gradlePluginPortal()
    }

    plugins {
        id "net.woggioni.gradle.osgi-app" version "0.1"
    }
}

rootProject.name = 'hello-osgi'